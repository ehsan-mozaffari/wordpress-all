services:
  wordpress-service:
    image: 6.4.3-php8.1-apache
    container_name: wordpress-container
    restart: unless-stopped
    env_file:
      - .env
    # Don't expose the port here. This is a template for this service.
    volumes:
      - wordpress-html-volume:/var/www/html


volumes:
  wordpress-html-volume: